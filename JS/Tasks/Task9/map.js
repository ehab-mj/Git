["a", "b", "c"].map((x, i) => {
    console.log(x, i);
});
// a 0
// b 1
// c 2

// 🔹 x = العنصر
// 🔹 i = ترتيبه في المصفوفة

// map -> map تمرّ على كل عنصر في المصفوفة
// array.map((element, index) => {
// });
// .map((c, d) -> هي أسماء متغيّرات فقط ->
// (تقدر تسميها أي اسم تحبه)